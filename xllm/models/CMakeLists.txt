include(cc_library)

if(USE_NPU)
  include_directories(
    ${CMAKE_SOURCE_DIR}/third_party/spdlog/include
  )
endif()

cc_library(
  NAME
    models
  HDRS
    model_registry.h
    models.h
    spec_feature_dump.h
    dit/utils/lanczos_resample.h
  SRCS
    model_registry.cpp
    spec_feature_dump.cpp
    dit/utils/lanczos_resample.cpp
  DEPS
    :model
    :model_context
    :dit_cache
    :model_loader
    :layers
    :util
    nlohmann_json::nlohmann_json
    $<$<BOOL:${USE_NPU}>:npu_layers>
)
